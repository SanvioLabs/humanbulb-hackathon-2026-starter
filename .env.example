# AWS Configuration
# Your AWS credentials come from `aws configure sso` + `aws sso login`
# Do NOT paste access keys here. The CLI will use your SSO session.
# Set this in your terminal: export AWS_PROFILE=hackathon

AWS_REGION=us-east-1
AWS_PROFILE=hackathon
PORT=3000
